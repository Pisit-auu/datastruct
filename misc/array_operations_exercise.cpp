#include <stdio.h>

int main() {
    int n;

    scanf("%d", &n);
    if (n > 20) return 1; // ????????????? n

    int think[n + 1]; // ?????????????????????????????
    for (int i = 0; i < n; i++) {
        scanf("%d", &think[i]);
        if (think[i] < 0 || think[i] > 100) return 1; // ?????????????????????
    }

    // a) ???????????????? 1 ??? 4
    printf("a) ");
    for (int i = 0; i < 4 && i < n; i++) {
        printf("%d ", think[i]);
    }
    printf("\n");

    // b) ?????????????????? think[2] ??? c) ???????????
    printf("b) ");
    for (int i = 0; i < n; i++) {
        if (think[2] < think[i]) {
            printf("%d\n", think[i]);
            printf("c) %d\n", i);
            break;
        }
    }

    // d) ???????? think[n] = think[2] + think[3]
    if (n >= 3) {
        think[n] = think[2] + think[3];
        n++; // ?????????????????
    }
    printf("d) ");
    for (int i = 0; i < n; i++) {
        printf("%d ", think[i]);
    }
    printf("\n");

    // e) ????????? think[3]
    if (n >= 5) {
        think[3] = (think[2] > think[4]) ? (think[2] - think[4]) : (think[4] - think[2]);
    }
    printf("e) ");
    for (int i = 0; i < n; i++) {
        printf("%d ", think[i]);
    }
    printf("\n");

    // f) ??????????????? 2 ??? 6
    if (n >= 6) {
        printf("f) %d\n", (think[1] > think[5]) ? 2 : 6);
    }

    // g) ???????????????????? 2 ?????????????
    if (n >= 2) {
        int temp = think[1];
        think[1] = think[n - 1];
        think[n - 1] = temp;
    }
    printf("g) ");
    for (int i = 0; i < n; i++) {
        printf("%d ", think[i]);
    }
    printf("\n");

    // h) ??????????
    printf("h) new= [");
    int New[n], index = 0;
    for (int i = 0; i < n; i++) {
        if (think[i] % 2 == 0) {
            New[index++] = think[i];
        }
    }
    for (int i = 0; i < index; i++) {
        printf("%d", New[i]);
        if (i < index - 1) printf(" ");
    }
    printf("]\n");

    return 0;
}

